providers:
  - Laltu\LaravelMaker\LaravelMakerServiceProvider

migrations:
  - database/migrations

workbench:
  start: '/'
  install: true
  welcome: true
  build:
    - asset-publish
    - migrate-refresh